# multipleTimes
To return a list of values whereby out of two parameters, the first is the number the sequence in the list starts with and the second is the number of times that same number is added to provide a sequence. 
